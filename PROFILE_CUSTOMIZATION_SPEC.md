# 프로필 배너 꾸미기 세부 기획서

작성일: 2026-09-18  
상태: **6개 슬롯의 첫 구현 완료, 로컬 검증 단계.** 배경·배너 프레임·배지·배지 프레임·칭호·칭호 프레임을 각각 선택한다. 운영 DB 적용 및 배포는 아직 하지 않았다. 아래 초기 제안 중 수량·신규 획득 조건·성능 목표는 확정 구현과 구분하며, 문서 마지막의 구현 현황을 우선한다.

기존 문서: [배너 구현 계획](./PROFILE_BANNER_IMPLEMENTATION_PLAN.md). 기존 배너 획득 조건과 지급 기록은 유지하고, 이 문서에서는 배너를 조합하는 기능을 확장한다. 개발 대상은 `web/`와 Supabase다.

## 1. 목표와 기본 원칙

플레이어가 자신이 좋아하는 배경, 게임에서 얻은 배지, 원하는 칭호를 조합해 개성을 표현한다. 상대는 배너를 보고 닉네임·실제 티어·대표 업적을 빠르게 알아볼 수 있어야 한다.

- 꾸미기는 능력치, 매칭, 기물 선택 범위, 승패에 영향을 주지 않는다.
- 슬롯마다 하나씩 선택한다. 같은 세트끼리만 조합하도록 제한하지 않는다.
- 획득한 아이템은 계정에 귀속하며 장치가 바뀌어도 유지한다.
- 실제 티어와 꾸미기 칭호를 분리한다. 칭호를 통해 현재 티어가 더 높은 것처럼 보이게 만들지 않는다.
- 첫 버전은 정적 이미지와 글자로 만든다. 상시 애니메이션·영상·파티클은 포함하지 않는다.
- 재화, 랜덤 상자, 거래, 유료 판매는 이번 범위에 포함하지 않는다.

## 2. 5개 슬롯의 역할

| 슬롯 | 바꾸는 부분 | 첫 버전 선택 규칙 | 표현 방식 |
| --- | --- | --- | --- |
| 배경 | 배너 전체의 바탕 그림 | 회원은 기존 6종 중 보유한 것 하나. 미선택 시 클래식 | WebP 이미지 |
| 배경 프레임 | 배너 바깥 테두리 | 하나 또는 없음 | CSS 테두리 또는 정적 투명 이미지 |
| 배지 | 왼쪽의 대표 업적 문양 | 하나 또는 없음 | 정적 SVG/WebP |
| 배지 프레임 | 배지 주변의 작은 장식 | 하나 또는 없음. 배지가 없으면 표시하지 않음 | CSS 또는 정적 SVG |
| 칭호 | 닉네임과 구분되는 짧은 이름 | 하나 또는 없음 | 번역된 고정 문구 |

배경 프레임은 배너 전체를, 배지 프레임은 배지 하나를 둘러싼다. 두 프레임의 위치와 역할이 겹치지 않게 제작한다. 닉네임 자유 입력 기능과 달리 칭호는 게임에서 제공하는 항목만 선택한다.

배지를 해제해도 선택한 배지 프레임은 기억한다. 배지가 없는 동안에는 프레임을 숨기고, 배지를 다시 선택하면 함께 표시한다. 화면에는 “배지를 선택하면 프레임이 표시됩니다”라고 안내한다.

## 3. 사용자별 이용 범위

| 사용자 | 표시와 선택 | 저장 |
| --- | --- | --- |
| 게스트 | 기존 단색 배너. 5개 슬롯의 실제 장착은 불가 | 회원 소유권이나 장착 기록을 만들지 않음 |
| 회원 | 기본 제공 및 획득한 아이템 조합 | 계정별 저장 |
| 봇 | 제작자가 지정한 고정 조합 | 사용자 계정 저장 없음 |
| 로컬 테스트 | 모든 조합의 외형 확인 가능 | 실제 보상·장착 기록에 쓰지 않음 |

게스트는 꾸미기 화면에서 아이템을 미리 볼 수 있지만, 적용 버튼은 회원 이용 안내로 바뀐다. 게스트 플레이 기록을 다른 회원 계정의 보상으로 가져오지 않는다. 로그아웃 시 단색으로 돌아가며 회원의 선택 기록은 지우지 않는다.

## 4. 게임 중 배치

상대 배너는 왼쪽 상단, 내 배너는 오른쪽 하단의 기존 위치를 유지한다.

```text
┌────────────── 배경 프레임 ──────────────┐
│  [배지 프레임]  닉네임           [친구+] │
│     [배지]     실제 티어                │
│                칭호                    │
└──────────── 배경 이미지 ────────────────┘
```

그림은 정보 배치를 설명하는 도식이다. 실제 배너 높이와 너비를 늘리라는 뜻은 아니다. 친구 버튼은 기존 조건에 해당하는 온라인 상대에게만 표시한다.

표시 우선순위는 닉네임 → 실제 티어 → 배지 → 칭호 → 장식이다. 내 차례 표시는 프레임 색상에 의존하지 않고 기존 턴 안내와 별도 표시로 유지한다.

### 모바일 규칙

- 현재 배너의 반응형 크기와 위치를 출발점으로 삼는다. 장식을 위해 게임판을 더 가리지 않는다.
- 배지는 기존 왼쪽 그림 영역 안에 넣는다. 기존 배경의 나이트 그림과 겹치는 경우 배지 밑에 작은 불투명 받침을 둔다.
- 닉네임과 티어는 각각 한 줄이다. 긴 닉네임은 말줄임표로 처리한다.
- 칭호는 일반 크기에서 세 번째 줄에 표시한다. 좁은 가로 화면에서 읽을 수 있는 글자 크기를 확보할 수 없다면 칭호를 숨긴다.
- 숨긴 칭호는 설정 미리보기와 프로필 상세에서 확인한다. 게임 중 상세 보기는 조작 영역을 넓히지 않도록 로비의 사용자 프로필 진입을 우선한다.
- 장식 레이어는 터치를 가로채지 않는다. 친구 버튼 등 실제 버튼만 터치를 받는다.
- 친구 버튼의 기존 터치 영역과 화면 가장자리 안전 여백을 보존한다.

## 5. 아트 제작 기준

기존 체스·목재·금속·문장 분위기를 유지한다. 배경에는 큰 제목이나 닉네임을 직접 그려 넣지 않는다. 실제 텍스트는 HTML로 표시해 언어 변경과 가독성을 유지한다.

| 자산 | 제작 기준 제안 | 주의점 |
| --- | --- | --- |
| 배경 | 3:1 비율, 990×330 내외 WebP, 파일당 60KB 이하 목표 | 가운데와 오른쪽은 글자를 읽을 수 있는 여백 확보 |
| 배경 프레임 | 늘어나도 모서리가 찌그러지지 않는 CSS 또는 9분할 이미지 | 그림자를 크게 확장하지 않고 배너 안쪽에 배치 |
| 배지 | 128×128 기준 정적 이미지, 화면에서는 약 28~40px | 작은 크기에서도 구별되는 실루엣 |
| 배지 프레임 | 배지 외곽에 얇게 표시, 같은 중심점 사용 | 배지 중앙을 가리지 않음 |
| 칭호 | 지원 언어별 고정 문자열 | 글자 수보다 실제 표시 너비로 확인 |

모든 배경은 밝은/어두운 글자색과 받침색을 함께 정의한다. 어두운 배경에 검은 글자가 놓이는 조합은 만들지 않는다. 이미지 실패 시에도 닉네임·티어는 불투명 바탕 위에 남아 있어야 한다.

기존 배경에는 테두리가 그림에 포함되어 있다. 처음에는 그 테두리 위에 얇은 장식을 덧대는 호환 방식을 사용한다. 프레임 두 겹이 어색하면 해당 배경의 가장자리만 수정하되 기존 배경 ID와 소유권은 유지한다. 신규 배경은 별도 프레임과의 조합을 전제로 제작한다.

## 6. 첫 버전 아이템 구성

새 업적을 대량으로 추가하기보다 기존 소유권을 활용해 5개 슬롯이 제대로 조합되는지 먼저 확인한다.

| 슬롯 | 첫 버전 구성 제안 | 획득 방식 |
| --- | --- | --- |
| 배경 | 클래식·슬레이트·포레스트·성운의 나이트·일도양단·불멸: 열두 척의 기적 | 기존 조건과 기록 유지 |
| 배경 프레임 | 없음·기본 황동·숙련의 문장 | 없음/황동은 회원 기본, 숙련의 문장은 기존 메달 8개 보상 연결 |
| 배지 | 없음 + 기존 숙련도 메달 배지 8종 | 기존 메달 달성 보상 유지 |
| 배지 프레임 | 없음·원형 링·방패 테두리 | 첫 버전은 회원 기본 제공 |
| 칭호 | 없음·도전자·탐험가 | 도전자는 회원 기본, 탐험가는 기존 메달 4개 보상 유지 |

기존 `frame:mastery-complete`는 **배경 프레임**으로 사용한다. 기존 배지와 칭호 ID도 바꾸지 않는다. 새 기본 제공 아이템의 ID는 구현 때 확정하고 이후 이름이 바뀌어도 유지한다.

이미 메달 4개·8개를 달성한 계정도 탐험가·숙련의 문장을 사용할 수 있어야 한다. 다시 달성하도록 요구하거나 같은 보상을 중복 지급하지 않는다.

향후 세트 예시는 다음과 같다. 아래 항목은 추가 콘텐츠 후보이며 이번 버전의 지급 약속이 아니다.

| 세트 | 배경 프레임 | 배지 프레임 | 칭호 후보 | 연결 방향 |
| --- | --- | --- | --- | --- |
| 성운 | 별자리 장식 | 별 모양 링 | 별을 읽는 자 | 나이트 관련 플레이 |
| 홍련 | 얇은 붉은 금속선 | 분할 문장 | 한 수의 파괴력 | 한 샷 다중 탈락 |
| 불멸 | 청동 해전 문양 | 닻 모양 장식 | 끝까지 남은 자 | 역전승 |
| 목재 | 조각 목재 | 나뭇잎 테두리 | 차분한 승부사 | 기존 목재 세트 권리와 연결 검토 |

세트는 검색·분류를 돕는 이름일 뿐이다. 세트를 모두 장착해도 능력치나 추가 보상을 주지 않는다. 현재의 목재 세트 예정 권리를 실제 구성 확정 없이 사용 완료로 처리하지 않는다.

## 7. 꾸미기 화면과 조작 흐름

진입점은 **설정 → 배너 꾸미기**로 한다. 숙련도 도감의 “장착”도 같은 장착 상태를 변경한다. 화면마다 서로 다른 선택을 저장하지 않는다.

1. 상단에 내 배너 미리보기를 보여준다. 실제 닉네임·티어를 사용한다.
2. 아래에서 배경 / 배경 프레임 / 배지 / 배지 프레임 / 칭호를 선택한다.
3. 해당 슬롯의 아이템 목록을 보고 하나를 누른다.
4. 누른 항목은 미리보기에만 반영한다.
5. **적용**하면 5개 슬롯을 한 번에 저장하고 실제 배너에 반영한다.

모바일의 5개 탭은 가로 스크롤을 허용하되 글자가 지나치게 작아지지 않도록 한다. “보유한 것만” 필터를 제공하고, 보유 아이템을 앞에 배치한다. 아이템 상세에는 이름·보유 여부·획득 조건·진척을 보여준다.

### 상태별 동작

| 상황 | 동작 |
| --- | --- |
| 잠긴 아이템 선택 | 미리보기는 가능. 획득 조건 표시, 적용은 불가 |
| 다른 슬롯에 잠긴 선택이 남음 | 적용 버튼을 비활성화하고 미보유 슬롯 안내 |
| 취소 또는 적용 전 닫기 | 마지막으로 적용한 조합 유지 |
| 모두 기본으로 | 미리보기만 기본 배경 + 나머지 없음으로 변경. 적용해야 저장 |
| 적용 성공 | 저장 완료 표시. 도감과 게임 배너도 같은 상태로 갱신 |
| 네트워크 실패 | 해당 계정의 로컬 대기 기록을 유지하고 저장 대기/재시도 표시 |
| 서버가 새 슬롯을 지원하지 않음 | 기존 서버 데이터를 보존하고 지원되지 않는 선택은 로컬 대기로 유지 |
| 계정 변경 | 이전 계정의 미리보기와 저장 요청을 분리하고 새 계정 상태를 불러옴 |

잠긴 아이템은 기본 조합으로 돌아가는 버튼을 제공한다. 저장이 끝나지 않았는데 완료 문구를 표시하지 않는다. 첫 버전은 변경 취소 이력이나 조합 프리셋 여러 개를 만들지 않는다.

## 8. 게임 모드와 상대 표시

- PvE: 내 조합과 봇의 고정 조합을 표시한다. 봇의 꾸미기는 보상 소유권과 무관하다.
- 온라인: 내 조합과 상대 계정의 공개 장착 조합을 표시한다. 상대의 비공개 진행 기록은 내려받지 않는다.
- 같은 기기의 2인 대전: 현재 계정의 조합은 기존 자기 쪽에만 사용하고 반대쪽은 단색을 유지한다.
- 튜토리얼·퍼즐: 기존 배너 표시 여부를 유지한다. 꾸미기 도입만으로 새 HUD를 추가하지 않는다.
- 온라인 상대 조합은 경기 시작·재접속 때 읽고 경기 중 반복 조회하지 않는다. 경기 도중 상대가 바꾼 조합은 다음 경기부터 반영해도 된다.
- 불러오기 실패 시 기본 조합을 표시한다. 이미지 실패나 장식 오류 때문에 매칭·기물 선택·턴 진행을 중단하지 않는다.

## 9. 계정 저장과 기존 데이터 호환

현재 코드는 `banner`, `frame`, `badge`, `title`의 4개 장착 슬롯을 지원한다. 사용자에게 보여주는 이름과 내부 저장 이름을 분리해 불필요한 데이터 이전을 줄인다.

| 사용자에게 보이는 슬롯 | 내부 키 제안 | 처리 |
| --- | --- | --- |
| 배경 | `banner` | 기존 유지 |
| 배경 프레임 | `frame` | 기존 유지, 의미를 명확히 정의 |
| 배지 | `badge` | 기존 유지 |
| 배지 프레임 | `badgeFrame` | 신규 추가 |
| 칭호 | `title` | 기존 유지 |

기존 `ca_mastery_progress_v1`, `ca_mastery_rewards_v1`, `ca_mastery_preferences_v1`을 확장하는 방향으로 한다. 새 슬롯이 없는 계정은 배지 프레임을 없음으로 읽는다. 기존 배경과 장착한 메달 배지는 유지한다.

저장에는 아이템 ID와 변경 시각 등 필요한 값만 넣고 이미지 파일·HTML·임의 CSS는 넣지 않는다. 기본 제공 아이템은 지급 이력을 무한히 만들지 않고 카탈로그의 기본 소유 규칙으로 처리한다.

### 서버 구현에서 반드시 처리할 사항

- 현재 클라이언트와 SQL에는 허용 슬롯·아이템·보상 조합 검증이 있다. UI만 추가해서는 저장할 수 없다.
- 신규 슬롯용 capability와 새 RPC 버전을 함께 정의한다. 기존 v3 호출 결과에 구버전이 해석하지 못하는 필드를 그대로 추가하지 않는다.
- 구버전의 읽기는 기존 필드만 보여주고, 구버전 저장은 새 슬롯과 새 보상 정보를 삭제하지 않도록 서버에서 보존한다.
- 같은 계정의 여러 기기에서 수정하면 기존 리비전 충돌 처리와 슬롯별 병합 규칙을 확장한다. 의도적인 “없음”도 변경 기록으로 취급한다.
- 공개 조회는 5개 장착 ID만 반환하는 새 응답을 제공한다. 기존 문자열 반환 `get_player_banner`는 구버전용으로 유지한다.
- 다른 슬롯의 아이템, 미보유 아이템, 알려지지 않은 ID는 서버에서 거부한다. 이미지 URL은 허용된 카탈로그에서만 가져온다.
- 신규 카탈로그의 소유권·획득 조건은 서버 검증 규칙과 함께 배포한다. 단순 색상 이미지 교체와 새 보상 규칙 추가는 서로 다른 작업이다.

지금의 클라이언트 PvE 기록 검증을 서버 권위의 경기 검증으로 설명하지 않는다. 향후 유료·거래 가능한 보상으로 확장한다면 지급 검증을 별도로 강화해야 한다.

## 10. 성능 예산과 실패 대응

아래 수치는 첫 구현에서 측정할 목표이며, 현재 보장된 성능 수치가 아니다.

| 항목 | 목표 |
| --- | --- |
| 내/상대 장착 장식 추가 다운로드 | 배경 포함 합계 250KB 이하 목표, 중복 파일 재사용 |
| 미리 불러오기 | 실제 장착한 자산만. 전체 컬렉션 사전 로딩 금지 |
| 선택 목록 | 보이는 썸네일부터 지연 로딩. 아이템 수가 늘면 페이지 단위 표시 |
| 게임 중 장식 갱신 | 상태 변경 때만 DOM 변경. 매 프레임 저장·통신·이미지 생성 금지 |
| 애니메이션 | 첫 버전 없음 |
| 레이어 | 배경·프레임·배지·배지 프레임의 소수 정적 요소로 구성 |
| 성능 비교 | 같은 기기·장면에서 기존 배너와 새 조합을 비교. 프레임 시간 p95가 반복 측정에서 5% 넘게 악화되면 원인 확인 후 출시 판단 |

다운로드 용량만으로 메모리를 판단하지 않는다. 압축 이미지도 표시할 때는 픽셀 크기에 따른 메모리가 필요하므로 대형 원본을 축소 표시하는 방식을 피한다.

배경 실패는 단색 대체, 프레임 실패는 숨김, 배지 실패는 중립 아이콘 또는 숨김으로 처리한다. 이미지 재시도를 매 프레임 실행하지 않는다. 이미지를 불러와도 배너의 크기가 바뀌지 않아야 한다.

## 11. 다국어와 접근성

- 이름·설명·획득 조건·진척·버튼·저장 상태를 기존 9개 언어에 제공한다.
- 칭호가 없으면 빈 줄을 강제로 남기지 않는다. 색상만으로 잠김·선택·내 차례를 구별하지 않는다.
- 선택 버튼에 이름과 선택 상태를 제공하고 키보드로 선택·적용·닫기가 가능해야 한다.
- 배지·프레임 이미지의 실패 여부와 관계없이 닉네임·티어를 읽을 수 있어야 한다.
- 일본어·독일어 등 실제 번역 문자열로 좁은 화면을 확인한다. 한글 기준 글자 수 제한만 사용하지 않는다.

## 12. 구현 단계

1. **로컬 외형 시제품:** 5개 슬롯과 미리보기 제작. 기존 배경·배지를 재사용하고 프레임은 간단한 정적 형태로 만든다. 계정 보상을 수정하지 않고 모바일에서 직접 확인한다.
2. **장착 기능:** 적용/취소, 게스트 구분, 도감 연동, 소유권 확인, 이미지 실패 처리를 구현한다.
3. **계정·서버:** 새 슬롯 검증, RPC 호환, 공개 장착 조회, 저장 충돌 및 실패 복구를 구현하고 SQL 적용을 검증한다.
4. **온라인·모바일 검증:** 서로 다른 두 계정의 조합, 재접속, 작은 화면, 실제 Android 기기의 성능을 확인한다.
5. **출시:** 검증한 SQL을 먼저 적용하고 구버전 호환을 확인한 뒤 웹을 배포한다. Android 앱은 별도 빌드·검증·출시 대상으로 다룬다.

이번 문서는 개발 계획이며 SQL 실행·커밋·푸시·배포를 수행했다는 뜻이 아니다.

## 13. 완료 판정 체크리스트

- [ ] 5개 슬롯이 서로 독립적으로 선택되고 모든 보유 조합을 적용할 수 있다.
- [ ] 배지가 없으면 배지 프레임이 숨겨지고, 다시 선택하면 복원된다.
- [ ] 기존 6개 배경과 메달 배지·탐험가·완주 프레임의 권리가 유지된다.
- [ ] 게스트, 회원 A, 회원 B의 선택과 저장 대기가 섞이지 않는다.
- [ ] 잠긴 아이템의 미리보기는 가능하지만 실제 장착·서버 저장은 불가하다.
- [ ] 취소는 저장하지 않으며 적용은 5개 슬롯을 일관되게 반영한다.
- [ ] 오프라인 변경 후 재접속, 다중 기기 충돌, 구버전 저장을 거쳐도 새 슬롯이 사라지지 않는다.
- [ ] 온라인에서 상대 조합을 정확하게 읽고 조회 실패 시에도 게임이 진행된다.
- [ ] 320px 세로 화면과 작은 가로 화면에서 닉네임·티어·친구 버튼·턴 안내가 겹치지 않는다.
- [ ] 장식이 기물 선택과 드래그를 가로채지 않는다.
- [ ] 이미지 404·느린 네트워크에서도 글자가 읽히고 배너가 깜빡이며 크기를 바꾸지 않는다.
- [ ] 지원 9개 언어에서 누락된 번역과 레이아웃 깨짐이 없다.
- [ ] 기존/신규 배너를 같은 조건으로 비교한 실제 모바일 성능 기록이 있다.
- [ ] 로컬 전체 해금 테스트 모드가 프로덕션 장착 권한이나 보상 지급에 영향을 주지 않는다.

## 추가 요청: 6번째 슬롯인 칭호 프레임

최초의 5개 슬롯에 칭호 프레임을 추가합니다. 칭호 글자와 독립적으로 선택하며 내부 키는 `titleFrame`입니다. 칭호 미표시 시 프레임도 숨기되 선택은 기억합니다. 프레임 없음이면 글자만 표시합니다. 이 추가 요청은 위의 5개 슬롯 범위보다 우선합니다.

## 현재 구현 현황 — 2026-09-18

설정 → 꾸미기에서 6개 항목을 선택한 뒤 **적용**을 누르면 한 번에 저장합니다. **취소**는 적용 전 선택을 되돌립니다. 계정이 바뀌면 이전 계정의 미적용 선택도 지웁니다.

| 항목 | 첫 구현의 선택지 |
| --- | --- |
| 배경 | 기존 기본 3종 + 기존 업적 3종 |
| 배너 프레임 | 없음, 기본 금색, 숙련도 완성 보상 |
| 배지 | 없음 또는 기존 숙련도 메달 8종. 첫 승리 배지는 이미지, 나머지는 기호 |
| 배지 프레임 | 없음, 금색, 은색, 보라색 |
| 칭호 | 없음, 기본 도전자, 기존 탐험가 보상 |
| 칭호 프레임 | 없음, 금색, 은색, 보라색 |

게스트는 단색 배너를 사용합니다. 회원은 보유 항목만 적용할 수 있습니다. 새로운 업적이나 지급 조건을 만들지 않고 기존 지급 기록을 이어 사용합니다. 제공된 이미지 시트의 모든 그림을 개별 아이템으로 등록한 상태는 아닙니다.

실제 게임의 내 배너와 상대 배너가 같은 렌더러로 조합됩니다. 상대 장착 정보는 매칭 때 한 번 조회하며 별도 주기 조회를 추가하지 않습니다. 작은 배너에서는 닉네임과 티어를 우선하고 칭호를 숨깁니다. 설정의 칭호 항목에는 별도 미리보기가 있습니다.

추가 제작 이미지 2개(배너 프레임·첫 승리 배지)는 WebP로 합계 약 53KB입니다. 배지/칭호 프레임은 CSS로 표시하며 상시 애니메이션은 없습니다.

계정 저장은 새 v4 RPC를 먼저 확인합니다. 이전 서버에서는 새 항목을 계정별 기기 캐시에 보관하고, 업데이트 후 저장 재시도로 동기화합니다. 이전 앱으로 저장해도 새 슬롯이 사라지지 않도록 SQL에 호환 처리가 있습니다. 실제 기기 간 저장 확인에는 운영 DB 적용이 필요합니다.

- 로컬 전체 아이템 미리보기: `http://127.0.0.1:5199/?bannerPreview=1` → 게스트 시작 → 설정 → 꾸미기 → 적용 → 게임 시작. 이 모드는 보상이나 계정 데이터를 변경하지 않습니다.
- 일반 회원/게스트 흐름: `http://127.0.0.1:5199/`
- 서버 적용 순서: [Supabase 설정 안내](supabase/PROFILE_COSMETICS_SETUP.md)
- 자동 검증: `npm --prefix web run check:cosmetics`, `npm --prefix web run check:progress`, `npm --prefix web run check:regressions`, `npm --prefix web run build`

### 배지 품질 후속 반영

위 첫 구현의 기호 배지를 사용자 제공 `BannerAssets/badge2.png`의 원본 그림 8종으로 교체했습니다. 게임과 설정 목록 모두 같은 그림을 사용하고, 기호는 이미지 실패 시에만 표시합니다. 8종 WebP 합계 약 146KB이며 아이템 ID·획득 조건·SQL에는 변경이 없습니다.
